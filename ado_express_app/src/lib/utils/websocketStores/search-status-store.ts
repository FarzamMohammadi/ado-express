// import { writable } from "svelte/store";
// import type { ISearchStatuses } from '../models/interfaces/isearch-statuses.interface';

// const createSearchStatusStore = () => {
//   const { subscribe, set, update } = writable<ISearchStatuses>({});

//   return {
//     subscribe,
//     updateStatus: (status: ISearchStatuses) => update(oldStatus => ({ ...oldStatus, ...status }))
//   };
// };

// export const searchStatusStore = createSearchStatusStore();
